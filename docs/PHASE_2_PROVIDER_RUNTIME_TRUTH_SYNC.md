# Phase 2 Provider Runtime Truth Sync

This document records a narrow doc truth sync after the provider/runtime hardening work landed on `main`.

## Purpose
Bring the remaining stale build docs into alignment with the current proven provider/runtime state.

## What changed
- updated `AGENTS.md`
- updated `services/eidon-orchestrator/README.md`
- added `docs/CANONICAL_EXTERNAL_REFERENCES.md`

## Why this matters
- the build repo should describe the system that actually exists
- stale “no live model” language is now false
- the build repo should reference the larger external canon without duplicating it
