# Phase 2 Repo Truth Surface Sync

This document records the top-level repo documentation sync needed after the Phase 2 scaffold outgrew the earlier repo-level descriptions.

## Purpose
Make the repo root truth surface match the actual proven Phase 2 build state.

## Why this matters
- the root `README.md` should not claim Phase 0 when the repo is operating as a proven Phase 2 scaffold
- `AGENTS.md` should not constrain the build to Phase 0 and Phase 1 after Phase 2 persistence and proof work is already merged
- repo-level docs should tell the truth about the live chain, persistence layers, and workflow discipline

## Current truth
This branch updates only top-level documentation surfaces.
It does not change runtime behavior.
