# Phase 2 Proof Surface Sync

This document records the repo-level proof surface sync needed after the integration test expanded to cover service health surfaces.

## Purpose
Make the root repo description match the current standard integration proof.

## Why this matters
- the root `README.md` should describe the real proof surface on `main`
- once service health surfaces are in the standard test, the repo-level truth surface should say so
- top-level docs should not lag behind the proven build state

## Current truth
This branch updates only repo-level documentation for the integration proof surface.
It does not change runtime behavior.
